/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/portfolioo",
  assetPrefix: "/portfolioo",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
